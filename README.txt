PRODUCT PARSER

run instructions:

type: java -jar ProductParser.jar at the command line to run the program.

ProductParser.jar should be located in the HtmlParser folder.

Output is stored in the output folder